import type { CollapseProps } from '@worldprinter/wdesign-core'

export type DataTableRowExpansionCollapseProps = Pick<
    CollapseProps,
    'animateOpacity' | 'transitionDuration' | 'transitionTimingFunction'
>
