import { Analytics } from '@vercel/analytics/react'

import { WDesignProvider } from '@worldprinter/wdesign-core'

export default function App({ Component, pageProps }) {
    return (
        <>
            <WDesignProvider>
                <Component {...pageProps} />
            </WDesignProvider>
            <Analytics />
        </>
    )
}
